def test_settings(app):
    """
    GIVEN a Flask application
    WHEN appsettings are loaded (__init__)
    THEN check that the values are loaded
    """

    # assert type(app.config["CERTIFICATE_TITLES"]) is list
    # assert len(app.config["CERTIFICATE_TITLES"]) >= 10

    # assert len(app.config["SESSION_KEY"]) >= 10
    # assert len(app.config["MENU_SUPPORT_URL"]) >= 10
    # assert len(app.config["MENU_DASHBOARD_URL"]) >= 10
    pass
