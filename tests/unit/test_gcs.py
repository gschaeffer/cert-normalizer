def test_gcs(app):
    """
    GIVEN a Flask application
    WHEN app settings are set
    THEN check gcs values
    """

    # assert len(app.config['ACHIEVEMENTS_BUCKET']) >= 6
    # assert len(app.config['ARTIFACT_STORAGE_PREFIX']) >= 2
    # assert type(app.config['DRIVE_SUBFOLDER_IDS']) is dict
    pass
