def test_drive(app):
    """
    GIVEN a Flask application
    WHEN app settings are set
    THEN check folder id values
    """

    # assert len(app.config['DRIVE_FOLDER_ID']) >= 10
    # assert type(app.config['DRIVE_SUBFOLDER_IDS']) is dict
    pass
