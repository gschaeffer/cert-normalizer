def test_docai(app):
    """
    GIVEN a Flask application
    WHEN log level is set
    THEN check the response is valid
    """
    # from app.modules import doc_ai

    # assert doc_ai.to_iso_format('gcp', 'Sep 1, 2023') == '2023-09-01'
    # assert len(app.config['DOCAI_PROCESSOR_ENDPOINT']) >= 10
    # assert len(app.config['DOCAI_PROCESSOR_ID']) >= 3
    pass
